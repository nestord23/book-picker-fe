---
import '../styles/global.css';
---

<main style="display: flex; flex-direction: column; justify-content: center; align-items: center; min-height: 100vh; text-align: center; padding: var(--spacing-lg);">
  <section style="max-width: 600px;">
    <h1 style="font-size: 3rem; margin-bottom: var(--spacing-md);">
      Bienvenido a <span style="color: var(--color-accent);">Book Picker</span>
    </h1>
    
    <p style="font-size: 1.1rem; margin-bottom: var(--spacing-xl); color: var(--color-text-secondary);">
      Descubre y organiza tu biblioteca personal con una plataforma moderna diseñada para lectores apasionados.
    </p>

    <div style="display: flex; gap: var(--spacing-md); justify-content: center; flex-wrap: wrap;">
      <a 
        href="/login" 
        style="
          padding: var(--spacing-md) var(--spacing-xl);
          background-color: var(--color-accent);
          color: #000;
          border-radius: var(--radius-md);
          font-weight: 700;
          text-transform: uppercase;
          letter-spacing: 0.05em;
          transition: all var(--transition-normal);
          display: inline-block;
          text-decoration: none;
        "
        class="btn-primary-link"
      >
        Iniciar Sesión
      </a>
      
      <a 
        href="/registro" 
        style="
          padding: var(--spacing-md) var(--spacing-xl);
          background-color: var(--color-bg-secondary);
          color: var(--color-text-primary);
          border: 1px solid var(--color-border);
          border-radius: var(--radius-md);
          font-weight: 700;
          text-transform: uppercase;
          letter-spacing: 0.05em;
          transition: all var(--transition-normal);
          display: inline-block;
          text-decoration: none;
        "
        class="btn-secondary-link"
      >
        Registrarse
      </a>
    </div>
  </section>
</main>

<style>
  :global(.btn-primary-link:hover) {
    background-color: var(--color-accent-hover);
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(34, 197, 94, 0.3);
  }

  :global(.btn-secondary-link:hover) {
    border-color: var(--color-accent);
    background-color: rgba(34, 197, 94, 0.1);
  }
</style>